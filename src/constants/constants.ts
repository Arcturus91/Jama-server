export enum UserType {
  USER = 'user',
  ADMIN = 'admin',
}

export enum ChefType {
  CHEF = 'chef',
}

export enum OrderStatus {
  requested = 'requested',
  onCooking = 'onCooking',
  onDelivery = 'onDelivery',
  completed = 'completed',
}
